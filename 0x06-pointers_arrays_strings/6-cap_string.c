#include "main.h"

/**
 *cap_string - capitalizes strings
 *@s: Is a string
 *Return: string.
 */
char *cap_string(char *s)
{
int i;
i = 0;
while(s[i] != '\0')
{
if ((s[i - 1] == ' ' || s[i - 1] == '\n' || s[i - 1] =='\t' || s[i -1] == ',' || s[i - 1] == ';' || s[i - 1] == '!' || s[i - 1] == '?' || s[i -1] == '"' || s[i - 1] == 
}
return (s);
}
